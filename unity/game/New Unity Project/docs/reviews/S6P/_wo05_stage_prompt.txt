跟进 Channel A。S6P-WO-05 阶段性汇报，不是 Gate Review，不要重复审 04C。

仓库 https://github.com/zqs1223041447/GAME-ZZZ main。实现 commit 即将 push。
ProdSim 同进程仍 FNV1A64:99f1bfd3f81c4fe6 V3 invalid=0。未 rebaseline。

本阶段补齐上次「未做」四项（均走 shipped API，无第二 oracle）：
1. Sync 同 plan ×120：Load/unload 增量 0，Texture2D instance ID 稳定。
2. SliceHud.NotifyPanel：Build→非 Build 释放 PassiveTreeTextures（P/Tab/Esc/关闭/切 Map/Craft 都改 s.Panel 后进 Draw）。
3. Chrome 边端点：G0–G3 × 1920/2560 edgeSetMismatch=0，maxEdgeErrPx≤0.0038（节点中心同样 ≤0.0038）。
4. PlayMode 23/23（关树释放 + 可选截图）。EditMode 531/531。S6PWo05 17/17。

尚未做、不宣称 READY_FOR_GATE_REVIEW：VRAM/tree-owned bytes 四态表、1920/2560×LOD0/1/2 导演截图全套、冷进程 ×3。

请裁定下一步：继续补 VRAM/截图/冷进程后 Gate，还是现在就可以 Gate，还是 STOP。不要采购/换框架。
